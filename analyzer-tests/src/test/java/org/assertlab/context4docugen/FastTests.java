package org.assertlab.context4docugen;

/**
 * Marker interface for fast unit/integration tests.
 * 
 * Usage:
 * - Run fast tests only: mvn test (default)
 * - Run fast tests explicitly: mvn test -Dgroups=org.assertlab.context4docugen.FastTests
 */
public interface FastTests {
}