# Pilot Evaluation Results

These are the first 20-repository pilot outputs from branch
`task/reduced-20-evaluation`. They are retained for audit only and should not be
used as the publication RQ1 run because the Gradle cohort was adjusted after
observing failures.

Use the active `evaluation/results/` directory for the publication rerun.
